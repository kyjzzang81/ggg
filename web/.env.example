# 웹앱만 쓰는 변수 (루트 .env.example과 동일 키 사용 가능)
# 복사: cp web/.env.example web/.env.local 후 값 입력

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 주변 탭 — 네이버 지도 JS v3 (클라이언트 ID, NCP 콘솔 Maps)
# VITE_NAVER_MAP_CLIENT_ID=

# 개발/검증용 UI (운영에서는 비워두면 됩니다)
# VITE_SHOW_TEST_DATA=1
# VITE_SHOW_PIONEER_PROGRESS=1
